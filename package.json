{
  "name": "text-utilities",
  "version": "1.0.0",
  "description": "Browser-based text tools. Static pages with no build step; this manifest exists only so the test suite can run under Node.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "serve": "python -m http.server 8000"
  },
  "engines": {
    "node": ">=20"
  }
}
